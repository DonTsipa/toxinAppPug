/*  Rooom.create({
    bedrooms:2,
    price:5000,
    bathrooms:1,
    beds:2
})
.then(post => console.log(post._id))
.catch((e) => {
  console.error('Unable to connect to Add Room');
  console.log(e);
  process.exit(1);
});*/